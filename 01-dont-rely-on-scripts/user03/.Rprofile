.First <- function() {
  options(stringsAsFactors = FALSE)
  ssshhhlib <- function(pkg) {
    suppressWarnings(suppressPackageStartupMessages(library(pkg, character.only = TRUE)))
  }
  invisible(sapply(c("dplyr"), ssshhhlib))
}
